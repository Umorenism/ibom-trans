import React from "react";

import Form from "../Form";

const Booking = () => {
  return (
    <div className="h-screen flex flex-col gap-12 justify-center w-full items-center duration-300 bg-slate-300 dark:bg-black">
      <Form />
    </div>
  );
};

export default Booking;
